import { MusicLocalization } from "@/common/Languages/Localization/Music";
import { describe, expect, it } from "vitest";

describe("DeveloperTools", () => {
	it("Should be Defined", () => {
		expect(MusicLocalization).toBeDefined();
	});
});
