import { ReactionRolesLocalization } from "@/common/Languages/Localization/ReactionRoles";
import { describe, expect, it } from "vitest";

describe("DeveloperTools", () => {
	it("Should be Defined", () => {
		expect(ReactionRolesLocalization).toBeDefined();
	});
});
