import { DeveloperToolsLocalization } from "@/common/Languages/Localization/DeveloperTools";
import { describe, expect, it } from "vitest";

describe("DeveloperTools", () => {
	it("Should be Defined", () => {
		expect(DeveloperToolsLocalization).toBeDefined();
	});
});
